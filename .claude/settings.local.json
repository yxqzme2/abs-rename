{
  "permissions": {
    "allow": [
      "Bash(git add *)",
      "Bash(git commit -m 'Fix Unraid template: split MP3 hand-off into Path and Variable configs *)",
      "Bash(git push *)",
      "Bash(git commit -m 'Fix MP3 hand-off: simple folder names and copy \\(not move\\) by default *)"
    ]
  }
}
